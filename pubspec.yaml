name: say_v
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^4.0.4
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  flutter_dotenv: ^5.1.0
  http: ^0.13.0
  firebase_core: ^2.32.0
  firebase_auth: 4.12.1
  cloud_firestore: ^4.15.5
  cloud_functions: ^4.6.5
  cached_network_image: ^3.3.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.0.0
  test: ^1.16.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/stores.json
    - assets/data/.env
    - test/test_data/stores.json
